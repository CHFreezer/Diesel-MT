# Teacher-only overlay dependencies. The overlay inherits the locked student
# PyTorch/Transformers stack but installs these packages into its own venv.
accelerate==1.14.0
compressed-tensors==0.17.1
loguru==0.7.3
psutil==7.2.2
pydantic==2.13.4
